// features/customerDisplay/contexts/CustomerDisplayContext.jsx

import { createContext } from "react";

// Create the context
const CustomerDisplayContext = createContext(null);

export default CustomerDisplayContext;
