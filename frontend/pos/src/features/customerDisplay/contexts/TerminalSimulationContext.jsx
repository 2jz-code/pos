// features/payment/contexts/TerminalSimulationContext.jsx

import { createContext } from "react";

// Create the context
const TerminalSimulationContext = createContext(null);

export default TerminalSimulationContext;
