# POS Frontend Environment Variables (Development Defaults - Safe to Commit)
# These are default values used when running in development mode.

# Default Backend API endpoint for development
VITE_API_URL=http://localhost:8000/api

# Default Backend WebSocket endpoint for development (e.g., for kitchen display)
VITE_WS_URL=ws://localhost:8001/ws

# Default Local Hardware Agent endpoint for development
# Ensure your local agent runs on this address/port with HTTPS enabled
VITE_HARDWARE_AGENT_URL=http://localhost:9001

# NOTE: Sensitive keys like Stripe Publishable Key should NOT be stored here.
# Use .env.local or .env.development.local for those.